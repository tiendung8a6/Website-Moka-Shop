import './buttonshort.css'

const ButtonShort = ({children}) => {
    return (
        <button className="button-shortad">
            {children}
        </button>
    );
}

export default ButtonShort;